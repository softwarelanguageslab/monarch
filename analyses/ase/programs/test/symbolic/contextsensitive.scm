;; This program fails to be analyzed precisely
;; in a context insensitive analysis because 
;; the variables in the path condition have 
;; an infinite abstract count due to abstraction.
;; 
;; It shows that context sensitivty also applies
;; for abstract symbolic execution.

