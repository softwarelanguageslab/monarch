Analyses
========

This directory specifies a number of analyses that do not belong to the core analysis framework. They are pinned specific versions of the analysis framework, so that development on core features of the framework can continue without breaking the subsequent users.
