module TypeLevel.HList where

import TypeLevel.List
import Data.Kind





-- | Create a sparse HList where only the value of the given type is inserted
-- class SparseInsert (x :: Type) (xs :: [Type]) where
--    insert :: x -> xs -> SparseHList xs

